import { LumbrerasConfig } from "./LumbrerasConfig.js";

var Configuracion = {
    /**
     * 
     *
     */
    GetConfiguracion() {
        let config = LumbrerasConfig;

        return config;
    },
}

export { Configuracion };
